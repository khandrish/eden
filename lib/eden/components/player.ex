defmodule Eden.Component.Player do
  alias Eden.Entity
  require Logger
  
  def init(_entity) do
  	# Logger.debug("Initializing #{__MODULE__} of `#{entity}`")
   #  entity
   #  |> Entity.add_key(__MODULE__, "puppet", nil)
  end
end