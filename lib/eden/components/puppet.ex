defmodule Eden.Component.Puppet do
  #alias Eden.Entity
  require Logger
  
  def init(_entity) do
  	# Logger.debug("Initializing #{__MODULE__} of `#{entity}`")
   #  entity
   #  |> Entity.add_key(__MODULE__, "allow", MapSet.new())
   #  |> Entity.add_key(__MODULE__, "deny", MapSet.new())
   #  |> Entity.add_key(__MODULE__, "puppeted", false)
   #  |> Entity.add_key(__MODULE__, "puppeteer", nil)
  end
end