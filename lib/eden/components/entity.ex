defmodule Eden.Component.Entity do
  #alias Eden.Entity, as: E
  require Logger
  
  def init(_entity) do
  	# Logger.debug("Initializing #{__MODULE__} of `#{entity}`")
  	# entity
  	# |> Entity.put_key(__MODULE__, "created", Timex.now)
  end
end