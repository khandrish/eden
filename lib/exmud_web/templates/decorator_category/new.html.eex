<h1>New Decorator category</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.decorator_category_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.decorator_category_path(@conn, :index) %></span>
